<main>
    <section>
     <div>        
        {{>nav}} 
        {{#with page}}
        <h1 class="example" hidden>{{title}} </h1>
        {{/with}}
    </div>

{{>header}}
<br>
{{#with hero}}
<div class="hero">
   <div class="hero-content">
        <h1 class="hero-title">{{title}} </h1>
        <p class="hero-text">{{text}} </p>
   </div>
   <img src="{{photo}}" alt="photo" class="hero-photo">
</div>
{{/with}}

<main class="blog">
    Blog
</main>

<div class="form">
    Form
</div>

<br>
{{>footer}}
</section>
</main>